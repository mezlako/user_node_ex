version: v1
kind: Service
metadata:
  name: demo-api
spec:
  runtime: node
  port: 3000 # or whatever port the app uses
  env:
    - name: NODE_ENV
      value: 'production'
  secrets: []
  build:
    dockerfile: Dockerfile
